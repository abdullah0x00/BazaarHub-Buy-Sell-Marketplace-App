<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.marketplace_app"
    android:versionCode="1"
    android:versionName="1.0.0"
    tools:ignore="MissingVersion">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Biometric permissions -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" tools:node="replace" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <uses-feature android:name="android.hardware.camera" android:required="false" tools:ignore="OldTargetApi" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" tools:ignore="OldTargetApi" />

    <application
        android:label="marketplace_app"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:extractNativeLibs="true"
        android:usesCleartextTraffic="true"
        tools:replace="android:label"
        tools:ignore="GoogleAppIndexingWarning,MissingApplicationIcon,UnusedAttribute,OldTargetApi,Instantiatable,HardcodedDebugMode">

        <meta-data
            android:name="io.flutter.embedding.android.FlutterAssetsDir"
            android:value="flutter_assets" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            tools:ignore="OldTargetApi,AppLinkUrlError">

            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" tools:ignore="UnknownAttribute" />
        </intent>
    </queries>
</manifest>
